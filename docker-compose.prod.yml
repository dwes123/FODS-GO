services:
  db:
    image: postgres:15-alpine
    container_name: fantasy_postgres
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: fantasy_db
      DB_NBA_NAME: ${DB_NBA_NAME:-fantasy_basketball_db}
    ports:
      - "127.0.0.1:5433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
      # init script runs only on fresh volumes; for existing prod, create DBs manually
      - ./scripts/init-databases.sh:/docker-entrypoint-initdb.d/init-databases.sh:ro

  app:
    build: .
    restart: always
    depends_on:
      - db
    environment:
      DATABASE_URL: postgres://${DB_USER}:${DB_PASSWORD}@db:5432/fantasy_db?sslmode=disable
      DATABASE_URL_NBA: postgres://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NBA_NAME:-fantasy_basketball_db}?sslmode=disable
      GIN_MODE: release
      CORS_ORIGIN: https://frontofficedynastysports.com
      BREVO_API_KEY: ${BREVO_API_KEY:-}
      SMTP_FROM: ${SMTP_FROM:-}
      SLACK_WEBHOOK: ${SLACK_WEBHOOK:-}
      GOOGLE_CLOUD_PROJECT: fantasy-435215
      GOOGLE_APPLICATION_CREDENTIALS: /root/app/service-account.json
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./service-account.json:/root/app/service-account.json:ro

  app-staging:
    build:
      context: ./staging
      dockerfile: ../Dockerfile
    restart: always
    depends_on:
      - db
    environment:
      DATABASE_URL: postgres://${DB_USER}:${DB_PASSWORD}@db:5432/fantasy_db_staging?sslmode=disable
      DATABASE_URL_NBA: postgres://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NBA_NAME:-fantasy_basketball_db}_staging?sslmode=disable
      GIN_MODE: release
      CORS_ORIGIN: https://app.frontofficedynastysports.com
      SMTP_HOST: ""
      SMTP_PORT: ""
      SMTP_USERNAME: ""
      SMTP_PASSWORD: ""
      SMTP_FROM: ""
      SLACK_WEBHOOK: ""
      GOOGLE_CLOUD_PROJECT: fantasy-435215
      GOOGLE_APPLICATION_CREDENTIALS: /root/app/service-account.json
    ports:
      - "127.0.0.1:8081:8080"
    volumes:
      - ./service-account.json:/root/app/service-account.json:ro

  caddy:
    image: caddy:2-alpine
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - app
      - app-staging

volumes:
  db_data:
  caddy_data:
  caddy_config: